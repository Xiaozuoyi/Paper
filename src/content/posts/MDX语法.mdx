---
title: mdx语法介绍
pubDate: 2024-04-03 08:00:00
categories: 
  - 技术
description: 'MDX 是一种 Markdown 语法扩展，它允许在 Markdown 中使用 JSX 语法。'
---
import Prompt from '~/components/Prompt.astro';
import Accordion from '~/components/Accordion.astro';

<Prompt template={"info"}>
  MDX代码不能直接在Markdown中使用，需要一个支持MDX的解析器，比如Gatsby、Next.js等。
  ```json
  {
    "name": "MDX",
    "version": "1.0.0"
  }
  ```
</Prompt>

<Accordion title="MDX介绍" open="true">
  <details>
    <summary>MDX 是什么？</summary>
    <p>MDX 是一种 Markdown 语法扩展，它允许在 Markdown 中使用 JSX 语法。</p>
  </details>
  <details>
    <summary>MDX 有什么优点？</summary>
    <p>MDX 可以让你在 Markdown 中使用 JSX 语法，这样你就可以在 Markdown 文件中直接嵌入 React 组件。</p>
  </details>
  <details>
    <summary>MDX 怎么用？</summary>
    <p>MDX 代码不能直接在 Markdown 中使用，需要一个支持 MDX 的解析器，比如 Gatsby、Next.js 等。</p>
  </details>
</Accordion>

## 1.Markdown语法
MDX 完全兼容标准的Markdown语法。
```mdx
# 这是一个一级标题

## 这是一个二级标题

- 列表项1
- 列表项2

**这是粗体文本**

_这是斜体文本_
```
<Accordion title="测试标题">
  <details>
    <summary>这是一个一级标题</summary>
    <p>这是一个二级标题</p>
  </details>
  <details>
    <summary>列表</summary>
    <p>1. 列表项1</p>
    <p>2. 列表项2</p>
  </details>
  <details>
    <summary>文本</summary>
    <p><strong>这是粗体文本</strong></p>
    <p><em>这是斜体文本</em></p>
  </details>
</Accordion>
## 2. 嵌入 JSX
你可以直接在MDX文件中嵌入 JSX。
```mdx
export default () => <div>Hello, world!</div>
```

## 3. 嵌入组件
你可以在MDX文件中引入组件，并可以像在jsx中一样传递props。
```mdx
import Chart from './Chart'

<Chart type="bar" data={myChartData} />
```
## 4.导出元数据
你可以在MDX文件中导出元数据，这些元数据可以被构建系统使用。
```mdx
export const metadata = {
  title: 'MDX Introduction',
  tags: ['mdx', 'guide']
};

# MDX Introduction
```
## 5.使用 Markdown 和 JSX 混合内容

```mdx
# 今天的天气

今天的天气是晴朗的，适合出去散步。

<MyWeatherComponent city="San Francisco" />

接下来是本周的天气预报：

<WeatherForecast />
```
## 6.JavaScript 表达式
你可以在MDX中使用JavaScript表达式。
```mdx
现在时间是 {new Date().toLocaleTimeString()}.

<Chart data={[5, 10, 15, 20]} />
```
现在时间是{new Date().toLocaleTimeString()}

## 7.使用 Markdown 格式的组件
使用 Markdown 格式的组件
```mdx
<MyComponent>
  # 这是一个标题
  这是一个段落。
</MyComponent>
```

